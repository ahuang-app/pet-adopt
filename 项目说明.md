# PetAdopt — 宠物领养平台 项目说明

> 温暖有爱的前端 SPA 应用，连接流浪动物与爱心领养人

---

## 项目概览

| 项目 | 说明 |
|------|------|
| **项目名称** | PetAdopt（宠物领养平台） |
| **类型** | 前端 SPA 应用（React 18） |
| **线上地址** | https://ahuang-app.github.io/pet-adopt/ |
| **源码仓库** | https://github.com/ahuang-app/pet-adopt |
| **技术栈** | React 18 + Vite + Tailwind CSS v3 + Framer Motion + React Router v6 |
| **图标库** | Lucide React |
| **字体** | Varela Round（标题） + Nunito Sans（正文），Google Fonts |
| **部署方式** | GitHub Pages（gh-pages 分支） |

---

## 视觉风格

**手绘插画风（Playful Illustration）** — 轻松活泼、温暖有爱

- 卡片带虚线边框 + 轻微随机旋转 (±2°)
- 按钮有阴影偏移（3px 3px 0）的印章按压感
- 分隔线用波浪形 SVG 代替直线
- 背景漂浮爪印装饰动画
- 圆润大圆角（卡片 20px，按钮 16px）

## 配色

| 角色 | Hex | 用途 |
|------|-----|------|
| Primary | `#F97316` | 按钮、CTA、强调 |
| Background | `#FEFCE8` | 页面底色（奶油黄） |
| Surface | `#FFFFFF` | 卡片底色 |
| Foreground | `#9A3412` | 正文文字 |
| Border | `#FDE68A` | 虚线边框 |

## 动效（Framer Motion）

| 类型 | 参数 | 触发 |
|------|------|------|
| 弹性入场 | spring (stiffness:200, damp:20) | 滚动进入视口 |
| 悬浮 | y: -4, scale: 1.03, 200ms | hover |
| 点击回弹 | scale 0.95 → 1.03 → 1.0 | tap |
| 列表交错 | stagger 0.05s/项 | 容器入场 |

---

## 路由结构

| 路由 | 页面 | 说明 |
|------|------|------|
| `/` | HomePage | 落地页（6 个区块） |
| `/pets` | PetListPage | 宠物列表 + 可交互筛选 |
| `/pets/:id` | PetDetailPage | 宠物详情 + 领养申请 |
| `/stories/:id` | StoryDetailPage | 领养故事详情 |

> 使用 HashRouter（`/#/pets/1`），保证 GitHub Pages 上 SPA 路由稳定

---

## 页面结构（HomePage 6 区块）

```
1. Hero              — 渐变背景 + 漂浮爪印 + 搜索栏 + CTA
2. Success Stories   — 3 则领养故事卡片
3. Pet Cards         — 可筛选宠物网格（4-6 只）
4. Shelter Partners  — 6 个合作收容所 logo
5. How It Works      — 3 步领养流程（波浪连接线）
6. Final CTA         — 橙色渐变 + 领养号召
```

---

## 文件结构

```
pet-adopt/
├── index.html                    # 入口 HTML
├── package.json                  # 依赖与脚本
├── vite.config.js                # Vite 构建配置（base: /pet-adopt/）
├── tailwind.config.js            # 自定义主题（warm 色板、字体、圆角）
├── postcss.config.js
├── .github/workflows/deploy.yml  # GitHub Actions 自动部署
├── src/
│   ├── main.jsx                  # React 入口（HashRouter）
│   ├── App.jsx                   # 路由配置
│   ├── index.css                 # Tailwind 指令 + 全局样式
│   ├── data/
│   │   ├── pets.js               # 8 只宠物 mock 数据
│   │   ├── stories.js            # 4 则领养故事
│   │   └── shelters.js           # 6 个收容所
│   ├── components/               # 通用组件
│   │   ├── Navbar.jsx            # 导航栏（路由高亮）
│   │   ├── Footer.jsx
│   │   ├── FloatingPaws.jsx      # 漂浮爪印动画
│   │   ├── ScrollReveal.jsx      # 滚动入场包装
│   │   ├── SearchBar.jsx         # 类型/地区搜索
│   │   ├── FilterBar.jsx         # 标签筛选
│   │   ├── PetCard.jsx           # 宠物卡片（弹入动效）
│   │   ├── StoryCard.jsx         # 故事卡片
│   │   └── StepCard.jsx          # 流程步骤卡片
│   ├── sections/                 # 首页区块
│   │   ├── HeroSection.jsx
│   │   ├── SuccessStories.jsx
│   │   ├── PetCardsSection.jsx
│   │   ├── ShelterPartners.jsx
│   │   ├── HowItWorks.jsx
│   │   └── FinalCTA.jsx
│   └── pages/                    # 页面
│       ├── HomePage.jsx
│       ├── PetListPage.jsx
│       ├── PetDetailPage.jsx
│       └── StoryDetailPage.jsx
```

---

## 数据模型

### Pet（宠物）

| 字段 | 类型 | 说明 |
|------|------|------|
| id | string | 唯一标识 |
| name | string | 名字 |
| type | 'dog' \| 'cat' \| 'other' | 类型 |
| breed | string | 品种 |
| age | string | 年龄描述 |
| size | 'small' \| 'medium' \| 'large' | 体型 |
| gender | 'male' \| 'female' | 性别 |
| location | string | 所在地 |
| description | string | 描述 |
| traits | string[] | 性格标签 |
| health | { vaccinated, neutered, microchipped } | 健康状态 |

### Story（领养故事）

| 字段 | 类型 | 说明 |
|------|------|------|
| id | string | 唯一标识 |
| petName | string | 宠物名 |
| petId | string | 关联宠物 ID |
| adopterName | string | 领养人 |
| quote | string | 简短引言 |
| fullStory | string | 完整故事 |
| date | string | 领养日期 |

### Shelter（收容所）

| 字段 | 类型 | 说明 |
|------|------|------|
| id | string | 唯一标识 |
| name | string | 名称 |
| location | string | 城市 |
| petCount | number | 收容数量 |

---

## 交互行为

| 组件 | 交互 | 行为 |
|------|------|------|
| Hero 搜索栏 | 选类型/地区 + 点击搜索 | 跳转 `/pets?type=xxx&location=xxx` |
| Hero CTA | 点击 | 跳转 `/pets` |
| StoryCard | 点击卡片 | 跳转 `/stories/:id` |
| PetCard | 点击卡片 | 跳转 `/pets/:id` |
| FilterBar | 点击标签 | 实时过滤卡片 + 动画过渡 |
| "申请领养" | 点击 | toast 弹窗提示 |
| "查看全部" | 点击 | 跳转 `/pets` |

---

## 命令速查

```bash
# 开发
cd pet-adopt && npm run dev

# 构建
npm run build

# 部署到 GitHub Pages
npm run deploy
# 或：源码推送 + gh-pages 分支推送
git push origin master
```

---

## 更新记录

| 日期 | 版本 | 说明 |
|------|------|------|
| 2026-05-17 | v1.0 | 初始版本：4 路由 + 6 区块落地页 + 手绘插画风格 + GitHub Pages 部署 |
